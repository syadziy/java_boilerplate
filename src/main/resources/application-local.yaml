# =========================
# LOCAL SDK OVERRIDES
# =========================
sdk:
  security:
    enabled: false
    method:
      enabled: false
    cors:
      enabled: false

  # =========================
  # LOCAL LOGGING OVERRIDES
  # =========================
  logging:
    structured:
      enabled: true
      auto-configure-format: false

logging:
  structured:
    format:
      console: ""